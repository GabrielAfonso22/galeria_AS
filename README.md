
Galeria de Artes Online: Python Azure Function + React + CI/CDEste projeto implementa uma solução de ponta a ponta (Full Stack Serverless) hospedada na Microsoft Azure, com versionamento e automação de deploy (CI/CD) via GitHub Actions.🚀 Visão Geral do ProjetoA Galeria de Artes Online é uma aplicação web que exibe obras de arte, consumindo dados fornecidos por uma API Python Serverless (Azure Function) que, por sua vez, consulta um banco de dados SQL. As imagens das obras são servidas diretamente de um Blob Storage.✨ Arquitetura (Azure)O projeto segue um modelo desacoplado (Serverless Architecture) na Azure.ComponenteFunçãoTecnologiaFrontendInterface do usuário (Galeria de Artes).React (Vite)Backend (API)Endpoint HTTP Trigger que consulta o banco de dados.Azure Function (Python)Banco de DadosArmazenamento dos metadados das obras.Azure SQL Database (Camada Básica)ArmazenamentoHospedagem das imagens das obras de forma pública e eficiente.Azure Blob StorageCI/CDAutomação completa do Build e Deploy.GitHub Actions🛠️ Detalhes da Implementação1. Backend (Azure Function Python)Endpoint: [URL_DA_SUA_API]/api/GetObrasFunctionTecnologia: Python com a biblioteca pyodbc para conexão com o SQL Server.Segurança: A string de conexão (DbConnectionString) é armazenada como uma Variável de Ambiente (Application Setting) na Azure, garantindo que as credenciais do banco de dados não estejam no código.Resposta: Retorna um array JSON contendo Nome, Artista, Descricao e URLImagem.2. Frontend (React com Vite)Tecnologia: React SPA (Single Page Application) com Vite.Design: Estilo Netflix (Dark Mode), com layout centralizado e cards em coluna.Funcionalidade: Implementa um Modal para visualização da imagem em tela cheia ao clicar no card, melhorando a experiência do usuário.Consumo da API: Utiliza o hook useEffect e a função fetch para consumir o endpoint da Azure Function.3. Banco de Dados e StorageAzure SQL: Contém a tabela Obras com metadados e o link para o Blob.Blob Storage: Container obras com acesso de leitura pública (Blob) para que o Frontend possa carregar as imagens diretamente.⚙️ CI/CD (GitHub Actions)O deploy do projeto é 100% automatizado, com um fluxo de trabalho separado e otimizado para cada componente:Frontend (Azure Static Web App)Workflow: .github/workflows/deploy-frontend.yml (gerado automaticamente pelo Azure).Ação: Dispara o build do React, publica os arquivos estáticos e automaticamente configura a rota /api/* para fazer proxy (encaminhar) as chamadas para a Azure Function.Backend (Azure Function)Workflow: .github/workflows/deploy-backend.ymlGatilho: Dispara sempre que há um push na pasta backend/**.Ação: Instala as dependências Python (requirements.txt), empacota o código e publica na Function App na Azure usando o AZURE_FUNCTION_PUBLISH_PROFILE (Secret do GitHub).🔗 Acesso PúblicoO projeto está hospedado e acessível publicamente no domínio do Azure Static Web App após a conclusão dos workflows.URL Pública: https://nice-tree-06fc1c60f.3.azurestaticapps.net/
